#include <stdio.h>
/**
 * main - entry point for func
 * @args: array length
 * @argv: array
 * Return: 0
 */
int main(int args, char *argv[] __attribute__((unused)))
{
printf("%d\n", args - 1);
return (0);
}
